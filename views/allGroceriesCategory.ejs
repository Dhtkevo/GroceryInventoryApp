<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= category[0].name %></title>
  </head>
  <body>
    <h1><%= category[0].name %> - Grocery Items</h1>
    <ul>
      <% for (let i = 0; i < groceries.length; i++) {%>
      <h2>
        <a href="/groceries/<%= groceries[i].id %>"><%= groceries[i].name %></a>
      </h2>

      <% } %>
    </ul>
    <button>
      <a href="/categories/<%= category[0].id %>/update">Update Category</a>
    </button>
    <button>
      <a href="/categories/<%= category[0].id %>/delete">Delete Category</a>
    </button>
  </body>
</html>
